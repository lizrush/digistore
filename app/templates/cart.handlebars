<h1>Your Cart</h1>

<div class='row'>
  <div class='col-lg-2'>
    <ul class='list-unstyled'>
      {{# each items }}
        <li>
          {{ this.product.name }}
        </li>
      {{/ each }}
    </ul>
  </div>
</div>
