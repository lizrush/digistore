<div class='row'>
  <div class='col-lg-2'>
    <h4>All Orders</h4>
    <ul class='list-unstyled'>
      {{# each }}
        <li>
          {{# link-to "order" this }}
            {{ customer_name }}
          {{/ link-to }}
        </li>
      {{/ each }}
    </ul>
  </div>
  <div class='col-lg-10'>
    {{ outlet }}
  </div>
</div>
