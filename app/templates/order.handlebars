<h1>Order {{ id }}</h1>
<div class='row'>
  <div class='col-lg-8'>
    <table class='table table-hover'>
      <thead>
        <th>Customer</th>
        <th>Email</th>
        <th>Status</th>
      </thead>
      <tbody>
          <tr>
            <td>
              {{ customer_name }}
            </td>
            <td>
              {{ email }}
            </td>
            <td>
              {{ status }}
            </td>
          </tr>
      </tbody>
    </table>
  </div>
</div>
